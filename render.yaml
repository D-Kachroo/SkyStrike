services:
  - type: web
    name: skystrike
    env: node
    plan: free
    buildCommand: npm install && npm run build:frontend
    startCommand: npm run start:node
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: skystrike
